using System; 
   class Program { 
 public static void Main (string[] args) {
  int [] numbers = {3, 19, 6, 1, 2, 11};
   int sum = 0;
   for (int i = 0; i < numbers.Length; i++) {
     sum += numbers[i];
   }
   Console.WriteLine(sum);
  }
}
